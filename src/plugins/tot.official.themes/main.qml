import QtQuick
import Totm

// Entry root for the official theme pack. The gallery itself lives in
// ui/ThemesSection.qml (appearance slot); this root only marks the pack.
Item {
    property string pluginId: ""
}
